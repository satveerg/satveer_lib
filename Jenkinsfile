pipeline {
   agent { label 'master' }
   stages {
       stage('write') {
           steps {
               script {
                   readYaml
                   
               }
           }
       }
   }
}
